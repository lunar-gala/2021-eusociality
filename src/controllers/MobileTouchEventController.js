/**
 * Responds to all touch and scroll events on mobile.
 */

export function onTouchEvent (event) {
  console.log('touch event', event);
}

export function onScrollEvent (event) {
  console.log('scroll event', event);
}